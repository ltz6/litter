name: Mobile CI

on:
  pull_request:
    paths:
      - 'Makefile'
      - '.gitmodules'
      - 'apps/android/**'
      - 'apps/ios/**'
      - 'patches/codex/**'
      - 'shared/rust-bridge/**'
      - 'shared/third_party/codex/**'
      - 'tools/scripts/**'
      - '.github/workflows/mobile-ci.yml'
  push:
    branches: [ main ]
    paths:
      - 'Makefile'
      - '.gitmodules'
      - 'apps/android/**'
      - 'apps/ios/**'
      - 'patches/codex/**'
      - 'shared/rust-bridge/**'
      - 'shared/third_party/codex/**'
      - 'tools/scripts/**'
      - '.github/workflows/mobile-ci.yml'

concurrency:
  group: mobile-ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  shared-prep:
    runs-on: macos-26
    timeout-minutes: 60
    env:
      HOMEBREW_NO_AUTO_UPDATE: "1"
      SCCACHE_BUCKET: rust-cache
      SCCACHE_ENDPOINT: ${{ secrets.SCCACHE_R2_ENDPOINT }}
      SCCACHE_REGION: auto
      SCCACHE_S3_USE_SSL: "true"
      SCCACHE_S3_KEY_PREFIX: ci/mobile-prep
      AWS_ACCESS_KEY_ID: ${{ secrets.SCCACHE_R2_ACCESS_KEY_ID }}
      AWS_SECRET_ACCESS_KEY: ${{ secrets.SCCACHE_R2_SECRET_ACCESS_KEY }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          submodules: recursive
          fetch-depth: 0

      - name: Select Xcode
        uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: '26.3'

      - name: Install build dependencies
        run: brew install meson ninja

      - name: Setup Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: aarch64-apple-ios-sim

      - name: Setup sccache
        uses: mozilla-actions/sccache-action@v0.0.9

      - name: Prepare shared mobile sources
        env:
          RUSTC_WRAPPER: sccache
          CARGO_INCREMENTAL: "0"
        run: |
          ./apps/ios/scripts/sync-codex.sh --preserve-current
          cd shared/rust-bridge
          ./generate-bindings.sh
          cd "$GITHUB_WORKSPACE"
          env -u CARGO_INCREMENTAL ./apps/ios/scripts/build-rust.sh --preserve-current --fast-sim --skip-bindings

          cp shared/rust-bridge/generated/swift/codex_mobile_client.swift \
            apps/ios/Sources/Litter/Bridge/UniFFICodexClient.generated.swift
          cp shared/rust-bridge/generated/swift/codex_mobile_clientFFI.h \
            apps/ios/GeneratedRust/Headers/codex_mobile_clientFFI.h
          cp shared/rust-bridge/generated/swift/codex_mobile_clientFFI.modulemap \
            apps/ios/GeneratedRust/Headers/codex_mobile_clientFFI.modulemap
          cp shared/rust-bridge/generated/swift/module.modulemap \
            apps/ios/GeneratedRust/Headers/module.modulemap

          mkdir -p .ci-prep/shared .ci-prep/ios-sim
          git -C shared/third_party/codex diff --binary > .ci-prep/shared/codex-submodule.patch
          tar -czf .ci-prep/shared/generated-mobile-sources.tgz -C . \
            shared/rust-bridge/generated \
            shared/rust-bridge/codex-mobile-client/src/types/codegen_types.generated.rs \
            shared/rust-bridge/codex-mobile-client/src/rpc/generated_client.generated.rs \
            shared/rust-bridge/codex-mobile-client/src/ffi/rpc.generated.rs
          tar -czf .ci-prep/ios-sim/ios-sim-rust.tgz -C . \
            apps/ios/GeneratedRust \
            apps/ios/Sources/Litter/Bridge/UniFFICodexClient.generated.swift

      - name: Upload shared prep artifact
        uses: actions/upload-artifact@v4
        with:
          name: shared-prep
          path: .ci-prep/shared
          if-no-files-found: error

      - name: Upload iOS simulator Rust artifact
        uses: actions/upload-artifact@v4
        with:
          name: ios-sim-rust
          path: .ci-prep/ios-sim
          if-no-files-found: error

  rust-bridge:
    needs: shared-prep
    runs-on: ubuntu-latest
    timeout-minutes: 40
    env:
      SCCACHE_BUCKET: rust-cache
      SCCACHE_ENDPOINT: ${{ secrets.SCCACHE_R2_ENDPOINT }}
      SCCACHE_REGION: auto
      SCCACHE_S3_USE_SSL: "true"
      SCCACHE_S3_KEY_PREFIX: ci/mobile-rust
      AWS_ACCESS_KEY_ID: ${{ secrets.SCCACHE_R2_ACCESS_KEY_ID }}
      AWS_SECRET_ACCESS_KEY: ${{ secrets.SCCACHE_R2_SECRET_ACCESS_KEY }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          submodules: recursive
          fetch-depth: 0

      - name: Install system deps
        run: sudo apt-get update && sudo apt-get install -y pkg-config libssl-dev libcap-dev clang cmake

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Setup sccache
        uses: mozilla-actions/sccache-action@v0.0.9

      - name: Download shared prep artifact
        uses: actions/download-artifact@v4
        with:
          name: shared-prep
          path: .ci-prep/shared

      - name: Restore shared prep
        run: |
          git -C shared/third_party/codex apply --binary .ci-prep/shared/codex-submodule.patch
          tar -xzf .ci-prep/shared/generated-mobile-sources.tgz -C .

      - name: Cargo check (codex-bridge)
        env:
          RUSTC_WRAPPER: sccache
          CARGO_INCREMENTAL: "0"
        run: cargo check --manifest-path shared/rust-bridge/codex-bridge/Cargo.toml

      - name: Cargo check (codex-mobile-client)
        env:
          RUSTC_WRAPPER: sccache
          CARGO_INCREMENTAL: "0"
        run: cargo check --manifest-path shared/rust-bridge/codex-mobile-client/Cargo.toml

      - name: Cargo test
        env:
          RUSTC_WRAPPER: sccache
          CARGO_INCREMENTAL: "0"
        run: make rust-test

  android:
    needs: shared-prep
    runs-on: ubuntu-latest
    timeout-minutes: 60
    env:
      SCCACHE_BUCKET: rust-cache
      SCCACHE_ENDPOINT: ${{ secrets.SCCACHE_R2_ENDPOINT }}
      SCCACHE_REGION: auto
      SCCACHE_S3_USE_SSL: "true"
      SCCACHE_S3_KEY_PREFIX: ci/mobile-android
      AWS_ACCESS_KEY_ID: ${{ secrets.SCCACHE_R2_ACCESS_KEY_ID }}
      AWS_SECRET_ACCESS_KEY: ${{ secrets.SCCACHE_R2_SECRET_ACCESS_KEY }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          submodules: recursive
          fetch-depth: 0

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3

      - name: Install Android platforms and NDK
        run: |
          yes | sdkmanager --licenses
          sdkmanager "platform-tools" "platforms;android-35" "build-tools;35.0.0" "ndk;30.0.14904198"

      - name: Install system deps
        run: sudo apt-get update && sudo apt-get install -y pkg-config libssl-dev libcap-dev clang cmake

      - name: Setup Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: aarch64-linux-android,armv7-linux-androideabi,x86_64-linux-android

      - name: Setup sccache
        uses: mozilla-actions/sccache-action@v0.0.9

      - name: Download shared prep artifact
        uses: actions/download-artifact@v4
        with:
          name: shared-prep
          path: .ci-prep/shared

      - name: Restore shared prep
        run: |
          git -C shared/third_party/codex apply --binary .ci-prep/shared/codex-submodule.patch
          tar -xzf .ci-prep/shared/generated-mobile-sources.tgz -C .
          mkdir -p .build-stamps
          touch .build-stamps/sync .build-stamps/bindings-kotlin

      - name: Install cargo-ndk
        run: cargo install cargo-ndk

      - name: Build Rust JNI libs
        env:
          ANDROID_NDK_HOME: ${{ env.ANDROID_HOME }}/ndk/30.0.14904198
          RUSTC_WRAPPER: sccache
        run: make rust-android

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Assemble debug
        run: apps/android/gradlew -p apps/android :app:assembleDebug

  ios:
    needs: shared-prep
    runs-on: macos-26
    timeout-minutes: 60
    env:
      HOMEBREW_NO_AUTO_UPDATE: "1"

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          submodules: recursive
          fetch-depth: 0

      - name: Select Xcode
        uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: '26.3'

      - name: Install build dependencies
        run: brew install xcodegen

      - name: Download shared prep artifact
        uses: actions/download-artifact@v4
        with:
          name: shared-prep
          path: .ci-prep/shared

      - name: Download iOS simulator Rust artifact
        uses: actions/download-artifact@v4
        with:
          name: ios-sim-rust
          path: .ci-prep/ios-sim

      - name: Restore shared prep
        run: |
          git -C shared/third_party/codex apply --binary .ci-prep/shared/codex-submodule.patch
          tar -xzf .ci-prep/shared/generated-mobile-sources.tgz -C .
          tar -xzf .ci-prep/ios-sim/ios-sim-rust.tgz -C .

      - name: Download ios_system frameworks
        run: ./apps/ios/scripts/download-ios-system.sh

      - name: Generate Xcode project
        run: ./apps/ios/scripts/regenerate-project.sh

      - name: Select simulator destination
        run: |
          SIMULATOR_UDID="$(
            xcrun simctl list devices available | awk -F '[()]' '
              /^-- iOS / { ios=1; next }
              /^--/ { ios=0 }
              ios && /iPhone 17 Pro/ { print $2; exit }
              ios && /iPhone/ && first == "" { first=$2 }
              END {
                if (first != "") {
                  print first
                }
              }
            '
          )"
          if [ -z "$SIMULATOR_UDID" ]; then
            echo "No available iPhone simulator found"
            xcrun simctl list devices available
            exit 1
          fi
          echo "IOS_SIMULATOR_UDID=$SIMULATOR_UDID" >> "$GITHUB_ENV"

      - name: Build (Litter)
        run: |
          xcodebuild \
            -project apps/ios/Litter.xcodeproj \
            -scheme Litter \
            -configuration Debug \
            -destination "id=$IOS_SIMULATOR_UDID" \
            ARCHS=arm64 \
            ONLY_ACTIVE_ARCH=YES \
            build
